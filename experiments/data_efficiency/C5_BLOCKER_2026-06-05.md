# C5 (Aryabumi code→text replica) — BLOCKED 2026-06-05

## Status
**C5 training cannot launch until you accept terms on a gated bigcode dataset.**

The other autonomous work (Aryabumi 0-shot re-eval on A5/B4, C5 training script) is still proceeding — only the data download is blocked.

## What blocked

I tried to download `bigcode/starcoderdata` (Aryabumi's exact code source — Stack v1 with Starcoder filters). Every parquet download returned **403 Cannot access gated repo**. Confirmed your HF token (`Dongwei / jiangdongwei0@gmail.com`) is valid but does not currently have access to:

| Repo | Status |
|---|---|
| `bigcode/starcoderdata` | gated, 403 |
| `bigcode/the-stack` | gated=auto |
| `bigcode/the-stack-dedup` | gated=auto, 403 confirmed |
| `bigcode/the-stack-v2` | gated=auto |
| `bigcode/the-stack-smol` | gated=auto |

For markup, the same — Stack-family markup is the canonical source per Aryabumi Table 4.

## Options for you to decide

**Option A — Get StarCoderData access (fully faithful to paper)**
- Go to https://huggingface.co/datasets/bigcode/starcoderdata, click "Access repository" → fill the form. Same for `bigcode/the-stack` if you want a fallback.
- Approval is usually automatic / fast. Once approved, I can resume the download immediately and finish the rest of the pipeline as planned.
- Net delay: depends on HF turnaround (minutes to hours).

**Option B — Authorize fallback to `codeparrot/github-code` (closest open source)**
- Same data source category as Stack (GitHub crawl), just from a different scrape and NOT Starcoder-filtered (no dedup, no quality filtering). Aryabumi explicitly says Starcoder filtering matters.
- Multi-language but rows are tagged inside parquets, not in path — needs filtering pass.
- Net difference: less-filtered code than what Aryabumi used. Some impact on absolute numbers, but the experimental shape (single-stage 25% mix vs `code→text` ordering, etc.) is preserved.
- I would NOT proceed with this without you explicitly saying so — per the "no silent alternatives" rule.

**Option C — Use what we already have tokenized (least faithful, fastest to launch)**
- Existing caches: `aryabumi_code_synth_full` (5.4B OCR with reasoning traces), `aryabumi_code_web` (1.35B Python from `codeparrot/github-code`), `opc_algorithmic` (0.94B).
- This is *exactly* what B4 already used — running C5 with this data is just "B4 with code-first ordering instead of mixed throughout". Compares B4's ordering vs C5's ordering with all other data held identical.
- This isn't a faithful Aryabumi replication; it's a code-mix-ordering ablation against our existing B4 baseline.

## What is still running

- ✅ **Aryabumi 0-shot evals on A5 + B4** (started 16:08 PDT) — running on 11 of 13 Aryabumi-suite tasks (missing QUAC = not in lm-eval, StoryCloze = HF-gated). Will land in a few hours.
- ⏸ **StarCoderData download** — killed (was hitting 403 on every file).
- 🔜 **C5 training script** — I'll still write this so it's ready to launch as soon as you decide the data path. Cache paths plug in last.

## My recommendation

A first (you do it). B second if A is slow. NOT C without you explicitly choosing it.

When you're back: pick A/B/C and I'll resume from there.
